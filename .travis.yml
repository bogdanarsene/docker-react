language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t bogdanarsene/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bogdanarsene/docker-react npm run test
